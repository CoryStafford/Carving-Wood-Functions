const addValues = (num1, num2) => {
    const math = num1 + num2 
    return math
}
const addSum = addValues(2, 3)
console.log(addSum)

const multiplyValues = (num1) => {
    const math = num1 * 2
    return math
}
const multiplySum = multiplyValues(5)
console.log(multiplySum)

addValues(4)
console.log(addValues)